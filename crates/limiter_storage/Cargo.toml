[package]
name = "limiter_storage"
version = "0.2.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.3", features = ["full"] }
redis = { version = "1.2.2", features = ["tokio-rustls-comp","json","connection-manager"] }
rustls = { version = "0.23" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

#limiter_core = {path="../limiter_core"}
